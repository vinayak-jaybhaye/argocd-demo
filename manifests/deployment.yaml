apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-html-app
  labels:
    app: my-html-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: my-html-app
  template:
    metadata:
      labels:
        app: my-html-app
    spec:
      containers:
        - name: my-html-container
          image: your-dockerhub-username/my-html-app:latest  # Use your pushed image
          ports:
            - containerPort: 80
